# FloatPaste v0.8.0 发布说明

发布日期：2026-09-16

### 修复

- 修复速贴面板行高抖动并统一浮层窗口样式处理
- 搜索窗口布局对齐旧版，修复导航滚动跟随与速贴互斥
- 搜索窗口行内图标重叠与 tooltip 层级修复
- 修正搜索窗口各处元素对齐偏移
- 搜索窗口小图标描边按显示尺寸折算，胶囊与快捷键框间距对齐旧版
- 搜索框在 IME 组合期间隐藏占位符，避免组合词与提示文字重叠
- 搜索框选区与 IME 组合词改走主题色，tooltip 显示不再夺走搜索窗焦点
- 加载失败态展示具体错误原因，对齐旧版文案
- 编辑器元信息插值改用 Slint 转义语法，标签输入行与删除按钮对齐旧版
- 编辑器打开期间速贴改为停屏屏幕外，修复返回后窗口全透明
- 编辑器窗口布局铺满与聚焦链修复
- 编辑窗口二次打开后头部与底栏露白
- 编辑窗口六项交互与布局对齐修复
- 搜索窗从编辑器返回后全透明
- 评审修复——浮层遮挡、armed 删除按钮、返回焦点监视与词边界折行
- 速贴/搜索显隐统一停屏复现，编辑器前台获取绕过前台锁
- 鼠标钩子后置消光标冻结，编辑器显式抬升 Z 序并确定性整帧重绘
- 编辑器移除 DWM 白色边框，搜索/速贴上屏前先完成首帧落盘
- 编辑器重开白底——show 前翻转整帧重绘覆盖层
- 会话键盘钩子常驻，修复进出编辑器一轮后快捷键全灭
- 设置窗口布局对齐旧壳，release 不再弹出控制台
- 设置窗口控件垂直居中与复选框勾号，对齐旧壳观感
- 速贴置顶守护与搜索窗首开居中，设置窗口交互细节对齐旧壳
- 主题预设三卡严格等宽等高，对齐旧壳 grid-cols-3
- 速贴置顶守护覆盖检测，搜索窗首开去闪烁，设置窗口交互细节四项
- 键位徽章墨迹溢出——if 条件布局不贡献首选宽导致徽章塌缩
- 对齐 @tauri-apps/api 至 2.11，修复 tauri build 版本一致性校验


### 文档

- 规划原生复刻迁移后续阶段路线图


### 新功能

- 新增 floatpaste-native 原生客户端，Slint 复刻速贴面板
- 原生壳复刻搜索窗口
- 复刻编辑窗口——文本编辑/标签管理/两段删除/未保存确认与返回流程
- 原生壳补齐设置窗口与系统托盘


### 重构

- 收敛 src-tauri 重复实现，明确迁移期目录职责

## 手动验证建议

本版本为桌面端 Tauri → 原生 Slint 壳迁移的收尾版本，搜索窗口、编辑窗口、设置窗口与系统托盘均已在原生壳复刻。建议按以下场景验收：

1. 速贴面板：主快捷键开关面板；↑↓ 选择、数字 1-9 直达、Enter 上屏到目标应用；悬停条目出预览；点击面板外部关闭。
2. 搜索窗口：Alt+S 开关；关键词过滤、标签/类型筛选；双击或 Enter 上屏；Ctrl+Enter 进编辑器并返回。
3. 编辑窗口：修改文本与标签；两段式删除；未保存时关闭出确认框；保存后返回原窗口。
4. 设置窗口：左侧导航点击与滚动跟随；快捷键录制（含冲突拦截）；主题模式/预设/强调色切换即时生效；历史记录上限等开关修改后 800ms 自动落盘；Esc 关窗前自动保存。
5. 系统托盘：左键单击打开设置；右键菜单五项（打开速贴面板 / 打开搜索 / 打开设置 / 暂停监听 / 退出）；切换监听后菜单文案与设置页同步。
6. 通用：release 版启动无命令行窗口；二次启动唤起已有实例面板。

## 已知限制

- 当前仅支持 Windows 10 / Windows 11 x64
- 未做代码签名，首次运行可能出现系统风险提示
- 若系统缺少 WebView2 运行时，程序可能无法正常启动

## 反馈格式

- 目标应用：
- 操作步骤：
- 实际结果：
- 预期结果：
- 是否稳定复现：
- 附件：录屏 / 截图 / 日志片段
