# FloatPaste

Windows 剪贴板历史管理工具的领域术语表。定义概念是什么，不描述实现。

## 窗口

**速贴**：
键盘优先的无焦点快速面板，热键唤出，展示最近条目，选择后上屏并归还前台。
_Avoid_: 剪贴板面板、浮窗、Picker（仅作代码名）

**搜索窗口**:
全功能的历史检索窗口，支持关键词、类型、标签过滤与条目全量操作。

**编辑窗口**:
编辑单个条目内容与标签的窗口，从速贴或搜索窗口以"当前项编辑"进入。

**固定显示**:
速贴常驻可见的模式，经托盘菜单开启与关闭。
_Avoid_: 钉住、常驻速贴

**停靠**:
固定显示的速贴被拖至屏幕边缘后吸附的形态：鼠标离开时收为细条，悬停或点击展开。
_Avoid_: 贴边隐藏、停靠面板

**悬浮气泡**:
悬停条目时出现的预览浮窗；也承载按钮等非列表元素的提示文案。
_Avoid_: tooltip 提示框、预览窗

## 交互

**上屏**:
把条目内容写入目标窗口的动作。
_Avoid_: 输出、发送、回贴

**粘贴触发方式**:
条目上屏的鼠标触发约定（单击 / 双击），用户可配置。

**粘贴为 X**:
上屏形态的命名族。主入口（Enter / 单击）按本体命名：粘贴为文本、粘贴为图片、粘贴为文件；次级入口（Shift+Enter）按变体命名：粘贴为图片路径、粘贴为文件路径（路径作为文本上屏）；文本的次级入口按其处理功能命名。
_Avoid_: 图片的次级上屏称「粘贴为文件路径」（应为粘贴为图片路径）、回贴

**管理员上屏**:
向管理员权限（高完整性）目标窗口上屏的能力。

**会话键**:
窗口内执行的键盘操作约定（上屏、编辑、收藏、关闭等动作键）。

**Win+V 接管**:
把 Win+V 的归属从系统剪贴板历史改为唤起本软件；一键开启、开启前警告、可一键回退。

## 数据

**条目**:
一条被监听入库的剪贴板内容（文本 / 图片 / 文件路径）。
_Avoid_: 记录、clip
