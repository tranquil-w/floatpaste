// Slint 构建入口：slint-build 的 compile() 每次调用会覆盖
// SLINT_INCLUDE_GENERATED，多窗口必须经由单一入口导出。
export { QuickPasteWindow, ClipRow } from "picker.slint";
export { PanelGeometry } from "picker.slint";
export { SearchWindow, SearchRow, SearchTagChip } from "search.slint";
export { SearchGeometry } from "search.slint";
export { TooltipWindow, TooltipMetaBadge } from "tooltip.slint";
export { EditorWindow, EditorTagSuggestion } from "editor.slint";
export { SettingsWindow, PresetCard, AccentSwatch, TagRowData } from "settings.slint";
export { Theme, Metrics } from "theme.slint";
